{
  "name": "speedbump",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "build": "web-ext build --overwrite-dest",
    "dev": "web-ext run"
  },
  "devDependencies": {
    "web-ext": "^8.0.0"
  }
}
